// decodium-ui — titolo di sezione dentro un pannello: WORKED BEFORE, QSL STATUS...
import QtQuick
import Decodium.UI

Text {
    color: Theme.secondaryColor
    font.family: Theme.monoFamily
    font.pixelSize: 11
    font.bold: true
    font.capitalization: Font.AllUppercase
}
